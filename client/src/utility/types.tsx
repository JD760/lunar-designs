export type builderSettings = {
    braceletSize: number,
    threadColour: string
}

export type beadItem = {
    _id: string,
    name: string,
    description: string,
    img: string
}

export type braceletSettings = {
    size: number,
    threadColour: string
    threadType: string
}

